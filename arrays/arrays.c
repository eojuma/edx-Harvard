#include <stdio.h>
#include <string.h>
int main(void){

    // Simple array creation
    int num[3];
    num[0]=2;
    num[1]=3;
    num[2]=4;
    int sum=0;
    for (int i=0;i<strlen(num);i++){
        sum+=num[i]
    }
}
